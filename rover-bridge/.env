# Backend URL for SignalR connection
BACKEND_URL=http://localhost:5000

# Rover ID (must match frontend and SignalR group)
ROVER_ID=ROVER-1

# Serial port configuration (depends on Arduino connection)
SERIAL_PORT=/dev/ttyACM0
BAUD_RATE=115200